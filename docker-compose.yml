services:
  logserver:

    hostname: "logserver"
    container_name: "logserver"

    environment:
      - "TZ=Europe/Paris"
      
    image: logserver:1

    logging:
      driver: "json-file"

          #ports:
          #      - "514:514/udp"
          #      - "9002:80/tcp"
    ports:
        - 7321:7321
        - 7322:7322
        - 7322:7322/udp
    restart: "always"

        #    volumes:
        #        - /sys/fs/cgroup:/sys/fs/cgroup:ro

        #    tmpfs:
        #        - /run
        #    tmpfs:
        #        - /tmp

version: "3.6"

